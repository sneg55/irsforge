/** Minutes after last publish before a curve is considered stale. */
export const CURVE_STALENESS_MINUTES = 10

/** Title shown in the HealthCard header. */
export const HEALTH_CARD_TITLE = 'Scheduler & oracle health'

/** Scheduler is considered stalled when last tick is older than STALE_MS (5 min). */
export const SCHEDULER_STALL_MS = 300_000

/**
 * Tests for blotter/mappers.ts have been split into:
 *   __tests__/mapper-helpers.test.ts  — getInstrumentCurrency / Direction / Maturity
 *   __tests__/mapper-rows.test.ts     — maturedToRow / terminatedToRow
 *
 * This file is intentionally empty; vitest discovers the split files via glob.
 */

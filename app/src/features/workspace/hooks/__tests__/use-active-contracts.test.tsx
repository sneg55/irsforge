/**
 * Tests for useActiveContracts have been split into:
 *   use-active-contracts-workflow.test.tsx — workflow resolution, parties, maturity, refetch
 *   use-active-contracts-effects.test.tsx  — margin, effects, terminate-proposal
 *
 * This file is intentionally empty; vitest discovers the split files via glob.
 */
